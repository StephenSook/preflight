{
  "name": "@preflight/web",
  "version": "0.1.0",
  "private": true,
  "description": "Preflight web: the cockpit dashboard (six screens), the public site, the browser sandbox checker, and the in-browser softphone.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@preflight/engine": "workspace:*",
    "@preflight/reference": "workspace:*",
    "@preflight/rules": "workspace:*",
    "@vonage/client-sdk": "2.7.0",
    "gsap": "^3.15.0",
    "lenis": "^1.3.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "axe-core": "^4.13.0",
    "vite": "^7.0.0"
  }
}
