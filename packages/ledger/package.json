{
  "name": "@preflight/ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Hash-chained evidence log with canonical serialisation, append-only verification, and Sigstore Rekor anchoring.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "@sigstore/sign": "^5.0.0"},
  "devDependencies": {}
}
